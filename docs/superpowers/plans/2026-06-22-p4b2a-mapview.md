# P4b-2a — MapView Decomposition (safe structural + test net) — Implementation Plan

> **For agentic workers:** Execute task-by-task via subagent-driven-development. MapView is
> Leaflet/jsdom-incompatible — the net is Playwright e2e + pure-helper unit tests. Build the e2e net
> FIRST. No behavior change. MapView's prop interface + the e2e CSS contract + localStorage keys are FROZEN.

**Goal:** Shrink + de-risk `MapView.tsx` (2867 LOC) via safe structural extraction, build the test net,
and close its view-rule violation — deferring the risky per-layer Leaflet hooks to P4b-2b.

**Spec:** `docs/superpowers/specs/2026-06-22-p4b2a-mapview-decomposition-design.md`

## Global Constraints

- Frontend-only, **no behavior change**. `MapView`'s prop interface FROZEN (`App.tsx` untouched).
- **Frozen e2e CSS contract**: `.leaflet-container`, `.current-pos-marker`, `path.route-flow-dash` (exact
  class names + presence/absence). **Frozen localStorage keys**: `locwarp.s2_enabled`/`s2_level`/`tile_layer`.
- After every commit: `cd frontend && npx vitest run` green + `npx tsc --noEmit` clean. After the
  dead-code delete + each structural extraction: `npx playwright test` green.
- Preserve the **wire-once + `*Ref`-mirror** pattern and `onMapReady({ panTo })`.
- Route api via `useServices().api` (MapView reads it; `useMapInstance` takes it as an arg). No new
  `useWebSocket`/`useWsRouter`. dependency-cruiser 0 errors throughout; MapView → scoped `error` at exit.

---

### Task 1: Branch + baselines + EXTEND the e2e net (FIRST)

**Files:** Modify `frontend/e2e/sim.spec.ts` / `ws.spec.ts` (or add `e2e/map.spec.ts`).

- [ ] Branch `feat/p4b2a-mapview` off main; pin `npx vitest run` count + `wc -l src/components/MapView.tsx` + `npx depcruise` baseline.
- [ ] Read the existing e2e harness (`routeWebSocket` mock + the frame shapes + selectors). Extend it to
  pin the Leaflet artifacts a refactor could move: the 4 leaflet-bar buttons (recenter/follow/library/S2
  exist in `.leaflet-bar`), the destination marker (drive the frame that creates it), and re-assert the
  route polyline base+arrow. Keep the exact selectors as the frozen contract. (Bookmark clustering is
  covered by a pure-helper unit test in Task 3, not e2e.) Pin only what's drivable via the existing mock.
- [ ] `npx playwright test` green. Commit: `test(p4b2a): extend e2e net for MapView leaflet-bar + destination marker`.

---

### Task 2: Delete dead dual-mode (~200 LOC)

**Files:** Modify `frontend/src/components/MapView.tsx` (+ `App.tsx` only if it threads now-unused props).

- [ ] Remove the `dualMode = false` constant (MapView.tsx:317), the per-device overlay effect/renderer
  (~1468-1637), and the `if (dualMode)` branches inside the single-device effects (current-position
  ~888-917, destination ~1010-1017, route ~1409, circle ~1448). Verify (read each) that `dualMode=false`
  makes every removed branch truly unreachable.
- [ ] If `devices`/`runtimes` become unused: prefer keeping MapView's prop interface stable (accept the
  props as unused / `void`) over changing the interface this round — do NOT modify App.tsx's call site
  unless the prop genuinely must go. Note the decision in the commit.
- [ ] `npx playwright test` + `npx vitest run` + `npx tsc --noEmit` green. Commit:
  `refactor(p4b2a): delete dead dual-mode code (~200 LOC, dualMode never true)`.

---

### Task 3: Extract pure helpers + unit tests (jsdom-safe)

**Files:** Create pure modules under `frontend/src/utils/` (or `services/`) + colocated `*.test.ts`;
modify `MapView.tsx` to import them.

- [ ] Extract the bookmark-pin **clustering** algorithm (px-distance grouping → single pin vs cluster) into
  a pure function taking the inputs it needs (points + a px→distance projector or pre-projected coords) and
  returning the cluster grouping. Unit-test the grouping (two near points cluster; far points don't; the
  threshold boundary).
- [ ] Move `haversineM` (MapView.tsx:245) + `escapeHtml` (235) to a shared util (next to `utils/coords`)
  + tests.
- [ ] Extract the **icon-HTML builders** (the `L.divIcon` HTML strings) into pure functions returning the
  HTML string + tests (assert the expected classNames/structure the e2e relies on, e.g. `current-pos-marker`).
- [ ] `npx vitest run` (grows) + `npx tsc` + `npx playwright test` green. Commit:
  `refactor(p4b2a): extract pure map helpers (clustering, haversine, icon builders) + unit tests`.

---

### Task 4: Extract `useMapInstance` (load-bearing)

**Files:** Create `frontend/src/hooks/useMapInstance.ts` + a thin test; modify `MapView.tsx`.

- [ ] Move the once-per-mount `L.map()` creation, default center, persisted initial-position fetch
  (`api.getInitialPosition` via the injected `api`), teardown, the map-level click/contextmenu/moveend/
  dragstart wiring, and `onMapReady({ panTo })` (MapView.tsx ~517-534, 681-702, 826-879). Signature e.g.
  `useMapInstance(containerRef, { onMapClick, onContextMenu, onMapCenterChange, onMapReady, api })`
  returning `{ mapRef }`. **Preserve the wire-once + `*Ref`-mirror pattern verbatim** (move the refs +
  their cleanup together). Everything else in MapView reads `mapRef`.
- [ ] `getInitialPosition` now comes from `api` (no dynamic `import('../services/api')`).
- [ ] `npx playwright test` (the net for the map-init order + buttons + initial marker) + `vitest` + `tsc`
  green. Commit: `refactor(p4b2a): extract useMapInstance hook (map lifecycle + onMapReady)`.

---

### Task 5: Extract `useBaseLayers(mapRef)`

**Files:** Create `frontend/src/hooks/useBaseLayers.ts`; modify `MapView.tsx`.

- [ ] Move the 6 tile-layer definitions + `L.control.layers` switcher + `baselayerchange ↔ localStorage`
  persistence (MapView.tsx ~704-824). Key `locwarp.tile_layer` FROZEN.
- [ ] `npx playwright test` + `vitest` + `tsc` green. Commit: `refactor(p4b2a): extract useBaseLayers hook`.

---

### Task 6: Extract `LeafletBarButton` primitive

**Files:** Create `frontend/src/components/LeafletBarButton.tsx` (or a `useLeafletBarButton` hook);
modify `MapView.tsx`.

- [ ] Collapse the 4 near-identical raw-DOM leaflet-bar button builders (recenter ~549-581, follow
  ~583-609, library ~611-637, S2 ~643-674) + their 4 React→DOM sync effects (~1655-1728) into one
  primitive (`icon`, `title`, `active`, `onClick`). Preserve the leaflet-bar markup the extended e2e pins
  + the visual button order (recenter, follow, library, S2).
- [ ] `npx playwright test` (the buttons net) + `vitest` + `tsc` green. Commit:
  `refactor(p4b2a): collapse 4 leaflet-bar buttons into a LeafletBarButton primitive`.

---

### Task 7: api inversion + MapView error-gate + final verify

**Files:** Modify `MapView.tsx`, `frontend/.dependency-cruiser.cjs`.

- [ ] MapView reads `const { api } = useServices()`; the still-inline context menu's `reverseGeocode`
  comes from `api.reverseGeocode`; remove the direct `import { reverseGeocode }` (line 3) and the dynamic
  `import('../services/api')` (now handled in `useMapInstance`). Confirm MapView imports nothing from
  `services/api`/`adapters` (`npx depcruise` no longer warns on MapView).
- [ ] Add MapView to a scoped dependency-cruiser **error** rule (extend `app-no-direct-api` or add
  `mapview-no-direct-api`). `npx depcruise` exits 0; verify it fails on an injected `services/api` import
  into MapView (then revert).
- [ ] Full `vitest` + `tsc` + `playwright test` green. Commit: `chore(p4b2a): MapView api-clean + enforce no-direct-api for MapView`.

---

### Final: whole-branch review + finish

- Dispatch the adversarial whole-branch review (behavior-equivalence on the map-init order + wire-once
  `*Ref` pattern + dead-code removal + the clustering helper; gate/DI; test quality of the e2e extension +
  pure-helper units; the frozen invariants). Fix confirmed findings (one wave).
- `finishing-a-development-branch`: full `vitest` + `tsc` + `playwright test` green → present merge options.
- Note P4b-2b (per-layer Leaflet hooks + popovers) remains for a future round.
