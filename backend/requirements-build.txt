# Pinned exactly: this repo has been bitten twice by PyInstaller copy_metadata
# bundle gaps (h3 / apple_compress / prompt_toolkit). A floating range can
# silently reintroduce that on a runner-side PyInstaller bump with no code change.
pyinstaller==6.21.0
